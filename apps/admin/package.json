{
  "name": "nest-music-admin",
  "version": "1.0.0",
  "private": true,
  "description": "Nest Music Admin Android app",
  "scripts": {
    "build:web": "node scripts/sync-web.js",
    "cap:sync": "npm run build:web && npx cap sync android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/app": "^6.0.3",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2",
    "@capacitor/status-bar": "^6.0.3"
  }
}
