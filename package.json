{
  "name": "nest-music",
  "version": "1.2.0",
  "private": true,
  "description": "Nest Music — lossless audio web + Capacitor Android apps",
  "scripts": {
    "build:admin": "npm --prefix apps/admin-web install && npm --prefix apps/admin-web run build",
    "build:web": "npm run build:admin && node scripts/prepare-public.js",
    "build": "npm run build:web",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
