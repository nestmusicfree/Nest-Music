{
  "name": "nest-music",
  "version": "1.0.0",
  "private": true,
  "description": "Nest Music — lossless audio web + Capacitor Android apps",
  "scripts": {
    "build": "mkdir -p public && cp www/index.html www/admin.html www/manifest.json public/",
    "vercel-build": "npm run build"
  }
}
